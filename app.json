{
  "environments": {
    "test": {
      "addons":[
         "heroku-redis",
         "heroku-postgresql"
      ],
      "scripts": {
        "test": "python -m black --check wiki_app wiki_parser wiki_race && python manage.py test"
      }
    }
  }
}
